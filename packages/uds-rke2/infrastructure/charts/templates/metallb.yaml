apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: prod
  namespace: {{ .Release.Namespace }}
spec:
  addresses:
    - {{ .Values.ipAddressPool.baseIP }}.{{ .Values.ipAddressPool.lowerBound }}-{{ .Values.ipAddressPool.baseIP }}.{{ .Values.ipAddressPool.upperBound }}
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: advertisement
  namespace: {{ .Release.Namespace }}
spec:
  interfaces:
    - {{ .Values.interface }}
